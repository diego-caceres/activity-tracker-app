<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Yellow dice background with rounded corners -->
  <rect x="5" y="5" width="90" height="90" rx="15" fill="#FFD700" stroke="#000" stroke-width="2"/>
  
  <!-- Black dots in a "5" pattern -->
  <!-- Top-left dot -->
  <circle cx="30" cy="30" r="7" fill="#000"/>
  
  <!-- Top-right dot -->
  <circle cx="70" cy="30" r="7" fill="#000"/>
  
  <!-- Center dot -->
  <circle cx="50" cy="50" r="7" fill="#000"/>
  
  <!-- Bottom-left dot -->
  <circle cx="30" cy="70" r="7" fill="#000"/>
  
  <!-- Bottom-right dot -->
  <circle cx="70" cy="70" r="7" fill="#000"/>
</svg>
